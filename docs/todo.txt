=> make memory read and write functions and get the serial outputs
=> understand the half carry overflows and full carry overflows
=> make flags for SUB
=> fix gameboydoc L register value
=> fix the flag bugs , H and C take old register values and Z takes result
---DONE---
=> make rest of the flag functions like INC
=> distribute the functions into different files
like with cpu_ld
---DONE---
=> fix the RR n implemenations
---DONE---
=> Make the serial I/O write to a file
=> Take in runtime arguments (need to do)
--NEW--
=> correct the BIT instructions and ig test them before doing 11 , also check the log of 11
--DONE--
=> Test 11th blargg's test
--DONE--
=> make a function showstack() (need to do)
--NEW--
=> write the docs of draw
=> implement colour in rendering
--DONE--
=> Complete writing the cycle data for each instruction
=> implement the interrupts (need to do)
--DONE--
=> Complete the game_init_sdl()
=> Complete the background debug
--DONE--
=> Check why isn't window not working in DMG_acid2 test






=====NEED TO DO========
=> Take in runtime arguments (need to do) (done) 
=> make a function showstack() (need to do)  (done) 
=> Make a config file which stores the ROM you want to
load with debug flags and other flags with serial IO
file location
